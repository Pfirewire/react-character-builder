import styled from "styled-components";

function QuickInfo() {
    return(
        <QuickInfoWrapper>
        </QuickInfoWrapper>
    );
}

export default QuickInfo;

const QuickInfoWrapper = styled.div`
  
`;