import styled from "styled-components";

function Skill() {
    return(
        <SkillWrapper>
            Skill
        </SkillWrapper>
    );
}

export default Skill;

const SkillWrapper = styled.div`
  
`;