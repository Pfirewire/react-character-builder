import styled from "styled-components";

function Skills() {
    return(
        <SkillsWrapper>
            Skills
        </SkillsWrapper>
    );
}

export default Skills;

const SkillsWrapper = styled.div`
  height: 360px;
`;