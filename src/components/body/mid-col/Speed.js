import styled from "styled-components";

function Speed() {
    return(
        <SpeedWrapper>
            Speed
        </SpeedWrapper>
    );
}

export default Speed;

const SpeedWrapper = styled.div`
  flex-grow: 1;
`;