import styled from "styled-components";

function Initiative() {
    return(
        <InitiativeWrapper>
            Initiative
        </InitiativeWrapper>
    );
}

export default Initiative;

const InitiativeWrapper = styled.div`
  flex-grow: 1;
`;