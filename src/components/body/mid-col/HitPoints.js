import styled from "styled-components";

function HitPoints() {
    return(
        <HitPointsWrapper>
            HitPoints
        </HitPointsWrapper>
    );
}

export default HitPoints;

const HitPointsWrapper = styled.div`

`;