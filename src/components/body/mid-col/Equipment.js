import styled from "styled-components";

function Equipment() {
    return(
        <EquipmentWrapper>
            Equipment
        </EquipmentWrapper>
    );
}

export default Equipment;

const EquipmentWrapper = styled.div`

`;