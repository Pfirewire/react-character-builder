import styled from "styled-components";

function FeaturesAndTraits() {
    return(
        <FeaturesAndTraitsWrapper>
            FeaturesAndTraits
        </FeaturesAndTraitsWrapper>
    );
}

export default FeaturesAndTraits;

const FeaturesAndTraitsWrapper = styled.div`

`;